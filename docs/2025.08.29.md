### 1. 编码问题
```
在切换conda环境后立即执行：
  setx PYTHONUTF8 1 #必须设置这个

并且所有python文件必须添加：【# -*- coding: utf-8 -*- 】

针对，setup中的long_description 明确指出使用
  long_description=open("README.md", "r", encoding="utf-8") .read(),
```

### 2. setup.py
```
 重新安装验证
pip uninstall -y medical_ai_xinhe  # 先卸载旧版本
pip install -e . --no-cache-dir
pip show medical_ai_xinhe | findstr "Version"
  > 输出Version: 0.1.0
```